# Install PyTorch first, matching your CUDA version: https://pytorch.org
# e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
torchvision>=0.15
numpy>=1.23
scipy>=1.9
Pillow>=9.0
PyYAML>=6.0
scikit-image>=0.20
lpips>=0.1.4
datasets>=2.14        # FFHQ parquet shards, inpainting task only
tqdm>=4.65
